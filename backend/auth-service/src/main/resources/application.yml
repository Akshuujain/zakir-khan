spring:
  application:
    name: auth-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-curly-butterfly-anzakonw.c-6.us-east-1.aws.neon.tech/ott_auth?sslmode=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_Uf6DcEP9ymGT}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

server:
  port: 8082
